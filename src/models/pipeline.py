"""ML training pipeline.

Implemented in a later phase. Phase 2 does not train models, scale, encode,
or apply SMOTE.
"""


def build_pipeline(*_args, **_kwargs):
    raise NotImplementedError(
        "Model training is planned for a later phase. "
        "Do not call this from Phase 2 validation."
    )
